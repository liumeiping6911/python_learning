直接运行py文件
在Mac和Linux上是可以 的，方法是在 .py 文件的第一行加上：
#!/usr/bin/env python
然后，通过命令：
$ chmod a+x hello.py 

print 会依次打印每个字符串，遇到逗号“,”会输出一个空格
print 也可以打印整数，或者计算结果

报错：
NameError: name 'raw_input' is not defined ---raw_input是2.x版本的输入函数，python 3用input
